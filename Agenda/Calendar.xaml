<Page
    x:Class="Agenda.Calendar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Agenda"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CanBeScrollAnchor="True">

    <Grid ManipulationMode="System" CanBeScrollAnchor="True" MinWidth="300" ScrollViewer.HorizontalScrollBarVisibility="Visible">
        <Button Content="Home" Margin="10,153,0,0" VerticalAlignment="Top" Click="Home_click" Width="100" FontSize="14" FontFamily="Constantia"/>
        <Button Content="Calendar" Margin="10,196,0,0" VerticalAlignment="Top" Width="100" FontFamily="Constantia" FontSize="14" Click="Calendar_click"/>
        <RelativePanel Margin="130,60,50,60">
            <CalendarView Height="550" Width="1055" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True"/>
        </RelativePanel>
        <Grid/>
    </Grid>
</Page>
